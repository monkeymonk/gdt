name = "dotnet"
version = "0.1.0"
protocol = 2
commands = ["dotnet"]
requires_gdt = ">=1.0"
description = "C# and .NET tooling for Godot projects"

[contributions]
hooks = ["after_new", "before_build", "before_export"]
doctor = true
completions = true
